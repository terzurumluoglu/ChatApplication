import { ConversationsHasMessagePipe } from './conversations-has-message.pipe';

describe('ConversationsHasMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ConversationsHasMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
