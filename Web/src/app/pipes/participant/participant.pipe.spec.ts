import { ParticipantPipe } from './participant.pipe';

describe('ParticipantPipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipantPipe();
    expect(pipe).toBeTruthy();
  });
});
