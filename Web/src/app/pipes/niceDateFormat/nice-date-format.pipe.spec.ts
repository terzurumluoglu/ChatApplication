import { NiceDateFormatPipe } from './nice-date-format.pipe';

describe('NiceDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NiceDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});